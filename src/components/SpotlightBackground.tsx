import { useEffect, useState } from 'react';
import { useTheme } from '../context/ThemeContext';

export default function SpotlightBackground() {
  // Disabled to eliminate mouse-hover fading and maximize site performance
  return null;
}
